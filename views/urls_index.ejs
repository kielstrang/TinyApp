<!DOCTYPE html>
<html lang="en">

<head>
  <title>TinyApp | Your URLs</title>
</head>

<body>
  <% include partials/_header %>
  <table>
    <% for (const id in userURLs) { %>
      <% const url = userURLs[id] %>
      <tr></tr>
        <form method="POST" action=<%= `/urls/${url.short}/delete?_method=DELETE` %>>
          <td><a href=<%= `/urls/${url.short}` %>><%= url.short %></a> &rarr; </td>
          <td><a href=<%= `/u/${url.short}` %>><%= url.long %></a></td>
          <td><input type="submit" value="Delete"></td>
          <td>Created on <%= url.created %>.  <%= url.analytics.visitCount %> visits from <%= url.analytics.uniqueVisitors.size %> unique visitors.</td>
        </form>
      </tr>
      <% } %>
  </table>
  <br>
  <a href="/urls/new">Shorten New URL</a>
</body>

</html>