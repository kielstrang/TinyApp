<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <table>
      <tr>
        <td>URL</td>
        <td>Short URL</td>
      </tr>
      <% for (const shortUrl in urls) { %>
        <tr>
          <td><%= urls[shortUrl] %></td>
          <td><%= shortUrl %></td>
        </tr>
      <% } %>
    </table>
</body>
</html>